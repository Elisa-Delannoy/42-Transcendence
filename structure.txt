---BACK END---

1️⃣Serveur de jeu en temps réel
Permet aux joueurs de se connecter et de jouer en direct.
Gère les connexions WebSocket pour les échanges rapides : positions, actions, événements de jeu.
Peut maintenir un état minimal du jeu côté serveur (position de la balle, score, etc.), ou juste relayer les infos si les clients calculent eux-mêmes la logique.
-> utiliser express et ws 

2️⃣ API REST (ou similaire) 
(= Representational State Transfer / style d’architecture qui utilise :
HTTP (GET, POST, PUT, DELETE…)
Des URLs pour identifier les ressources (/login, /users, /score)
Des JSON pour les échanges de données.)

Sert à transmettre des données “classiques” entre frontend et backend.
Exemple d’utilisation :
Authentification / login / signup
Récupération des informations de profil
Statistiques ou classement
En pratique, le front-end envoie des requêtes HTTP, et le backend répond avec JSON.


CRUD = Create, Read, Update, Delete
C’est un type d’opérations qu’une API peut fournir sur des données.
CRUD n’impose aucune architecture spécifique, juste que tu peux :
-Créer des données (Create)
-Lire des données (Read)
-Modifier des données (Update)
-Supprimer des données (Delete)

CRUD = opérations sur les données
REST = style d’API qui peut utiliser CRUD pour ses ressources
💡 En pratique : pour ton POC, ton backend REST pour les joueurs sera souvent CRUD + quelques actions spécifiques au jeu (ex : lancer partie, bouger balle).


3️⃣ Base de données
Stocke les informations persistantes :
Logins / mots de passe (hashés)
Scores, statistiques
Configuration du joueur (pseudo, avatar, etc.)
Le backend interagit avec la base pour authentifier et récupérer les infos.

⚠️ Ce qu’il ne faut pas oublier
Même si la logique du jeu peut être partiellement côté client, le backend doit rester l’autorité pour certaines données (score, login, matchmaking).
Sécurité : mots de passe hashés, vérification des requêtes, éviter que des clients trichent.


---FRONT END---

1️⃣ Interface utilisateur (UI)
Pages HTML/CSS pour afficher :
Écran de login / signup
Lobby ou liste des joueurs disponibles
Zone de jeu (canvas, div, etc.)
Scores et statistiques
L’UI doit être responsive et interactive, mais tout calcul lourd du jeu est géré côté JS.

2️⃣ Logique de jeu (Game Logic)
Partie JavaScript qui fait tourner le jeu dans le navigateur :
Mouvement des objets (balles, paddles, personnages…)
Détection des collisions
Gestion des scores temporaires (affichage, pas forcément persistants)
Envoie les actions au backend via WebSocket :
Exemple : “Joueur1 a déplacé son paddle en x=50”
Reçoit les mises à jour du backend pour afficher l’état des autres joueurs ou de la balle.

3️⃣ Communication avec le backend
WebSocket pour le jeu en temps réel :
Positions, actions, événements de jeu
Permet de synchroniser plusieurs joueurs
API REST pour les données persistantes :
Login / signup
Récupération du profil / stats / classement

4️⃣ Stockage temporaire côté client
Peut garder des données session-based :
Pseudo du joueur
Couleur du paddle ou avatar
Score temporaire pour affichage instantané
Attention : les données importantes (scores finaux, login) doivent être validées côté backend pour éviter la triche.\

